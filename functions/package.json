{
  "name": "readint-functions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The /api/define endpoint. The only server ReadInt has.",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-functions": "^6.6.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
